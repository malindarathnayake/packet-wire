# InfluxDB Configuration
# Required: Connection details for your InfluxDB instance
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=your-influxdb-token-here
INFLUX_ORG=your-org-name
INFLUX_BUCKET=your-bucket-name

# Optional: Skip TLS certificate verification
# Set to 'true' for internal PKI or self-signed certificates
# WARNING: Only use in trusted environments (dev/internal networks)
# Default: false
INFLUX_SKIP_TLS_VERIFY=false

# Dashboard Configuration
# Port the dashboard HTTP server will listen on
# Default: 8080
DASHBOARD_PORT=8080

